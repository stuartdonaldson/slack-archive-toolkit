#!/mnt/c/dev/venvs/uv1/bin/python
"""Repo-root convenience entry point - equivalent to `python -m slackbackup.cli`,
run with the project's WSL2/Linux venv interpreter (see ~/.claude/CLAUDE.md §9).
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))

from slackbackup.cli import main

if __name__ == "__main__":
    sys.exit(main())
